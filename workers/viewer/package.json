{
  "name": "@wedding-gallery/viewer",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --var ALLOW_LOCALHOST_CORS:true --var ENABLE_PRESIGNED_URLS:true --remote",
    "dev:local": "wrangler dev --var ALLOW_LOCALHOST_CORS:true",
    "deploy": "wrangler deploy"
  }
}
