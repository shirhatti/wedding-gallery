{
  "name": "wedding-gallery-frontend",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev --var ALLOW_LOCALHOST_CORS:true",
    "dev:remote": "npx wrangler dev --var ALLOW_LOCALHOST_CORS:true --remote",    
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0"
  }
}
